name: Sync Notion to README

on:
  schedule:
    - cron: "0 * * * *" # 매시간 0분마다 실행
  workflow_dispatch: # 수동 실행 가능

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ 리포지토리 체크아웃
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Node.js 설치 (Notion2GitHub 사용)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3️⃣ Notion2GitHub 설치
      - name: Install Notion2GitHub
        run: npm install -g notion2github

      # 4️⃣ Notion 문서 가져와서 README.md 갱신
      - name: Run Notion Sync
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_PAGE_ID: ${{ secrets.NOTION_PAGE_ID }}
        run: |
          notion2github -t $NOTION_TOKEN -p $NOTION_PAGE_ID -f README.md

      # 5️⃣ 변경사항 커밋 & 푸시
      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "actions@github.com"
          git add README.md
          git commit -m "🔄 Update README from Notion" || echo "No changes"
          git push
